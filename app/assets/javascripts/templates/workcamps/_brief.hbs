<div class="list-group">
  {{#each}}
    {{#link-to 'workcamp' this class="list-group-item"}}
    <h4 class="list-group-item-heading">
      {{flag country}}
      {{code}} - {{name}}
    </h4>
    <p class="list-group-item-text">
      {{ long-date begin}} - {{ long-date end}}
      <b>{{ format-places this }}</b>
    </p>
    {{/link-to}}
  {{/each}}
</div>
