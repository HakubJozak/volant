    <button type="submit" class="btn btn-primary">
      Save
    </button>
    {{#if isDirty }}
      <button type="button" class="btn" {{action "rollback" }}>
        Cancel
      </button>
    {{/if}}
