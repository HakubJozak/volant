<h1>{{name}}</h1>

{{#link-to 'workcamps'}}Back{{/link-to}}

<form class="form">

      {{view "field" attr='name'}}
      {{view "field" attr='code'}}
      {{view "date" attr='begin' }}
      {{view "date" attr='end' }}
      {{view "textarea" attr='description' }}


        <button type="submit" class="pure-button pure-button-primary">Save</button>

</form>