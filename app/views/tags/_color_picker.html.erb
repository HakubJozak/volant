  <dd>
    <%=
       html_id = "tag-#{field}-#{record.id}"
       text_field :record, field,
                  :id => html_id,
                  :onclick => "showColorPicker(this,$('#{html_id}'))", :class => 'text-input'    -%>
  </dd>
